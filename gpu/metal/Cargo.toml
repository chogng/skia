[package]
name = "skia-metal"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
metal = "0.33.0"
skia-core = { path = "../../core" }
skia-gpu = { path = ".." }

[dev-dependencies]
skia-image = { path = "../../image" }
