name: test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  linux:
    name: Linux Rust and Unicode conformance
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Format
        working-directory: skia-rs
        run: cargo fmt --all -- --check
      - name: Lint
        working-directory: skia-rs
        run: cargo clippy --workspace --exclude skia-metal --all-targets --all-features -- -D warnings
      - name: Workspace tests
        working-directory: skia-rs
        run: cargo test --workspace --exclude skia-metal --exclude skia-vulkan --all-features
      - name: Download pinned Unicode conformance data
        working-directory: skia-rs
        run: ../scripts/fetch_unicode_conformance.sh
      - name: Unicode conformance
        working-directory: skia-rs
        env:
          SKIA_UNICODE_CONFORMANCE_DIR: target/unicode-conformance
        run: cargo test -p skia-text --test unicode_conformance -- --ignored

  windows:
    name: Windows Rust and Vulkan compile coverage
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint
        working-directory: skia-rs
        run: cargo clippy --workspace --exclude skia-metal --all-targets --all-features -- -D warnings
      - name: Workspace tests
        working-directory: skia-rs
        run: cargo test --workspace --exclude skia-metal --all-features

  vulkan-lavapipe:
    name: Vulkan (Lavapipe + validation)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install software Vulkan and validation layers
        run: |
          sudo apt-get update
          sudo apt-get install --yes mesa-vulkan-drivers vulkan-tools vulkan-validationlayers
      - name: Select Lavapipe ICD
        run: |
          icd="$(find /usr/share/vulkan/icd.d -maxdepth 1 -name 'lvp_icd*.json' -print -quit)"
          test -n "$icd"
          echo "VK_DRIVER_FILES=$icd" >> "$GITHUB_ENV"
          echo "VK_ICD_FILENAMES=$icd" >> "$GITHUB_ENV"
      - name: Inspect Vulkan runtime
        run: vulkaninfo --summary
      - name: Vulkan tests
        working-directory: skia-rs
        env:
          SKIA_REQUIRE_VULKAN_DEVICE: "1"
          SKIA_VULKAN_VALIDATION: "1"
        run: cargo test -p skia-vulkan -- --nocapture --test-threads=1

  macos:
    name: macOS Rust and Metal (device optional)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint
        working-directory: skia-rs
        run: cargo clippy --workspace --exclude skia-vulkan --all-targets --all-features -- -D warnings
      - name: Workspace tests
        working-directory: skia-rs
        run: cargo test --workspace --exclude skia-vulkan --all-features
